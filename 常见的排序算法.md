|   **算法**   | **平均时间复杂度** | **最好时间** | **最坏时间** | **空间复杂度** | **稳定性** | **主要应用场景**                     |
| :----------: | ------------------ | ------------ | ------------ | -------------- | ---------- | ------------------------------------ |
| **冒泡排序** | O(n^2)             | $O(n)$       | $O(n^2)$     | $O(1)$         | **稳定**   | 教学演示，几乎不用于生产             |
| **选择排序** | O(n^2)             | $O(n^2)$     | $O(n^2)$     | $O(1)$         | 不稳定     | 交换成本极高且数据量小时             |
| **插入排序** | O(n^2)             | $O(n)$       | $O(n^2)$     | $O(1)$         | **稳定**   | **接近有序的数据**；小数组（n < 15） |



| **算法**     | **平均时间复杂度** | **最好时间**  | **最坏时间**  | **空间复杂度** | **稳定性** | **主要应用场景**                 |
| ------------ | ------------------ | ------------- | ------------- | -------------- | ---------- | -------------------------------- |
| **快速排序** | $O(n \log n)$      | $O(n \log n)$ | $O(n^2)$      | $O(\log n)$    | 不稳定     | **绝大多数通用场景**，内存占用少 |
| **归并排序** | $O(n \log n)$      | $O(n \log n)$ | $O(n \log n)$ | $O(n)$         | **稳定**   | 外部排序（大文件）；链表排序     |
| **堆排序**   | $O(n \log n)$      | $O(n \log n)$ | $O(n \log n)$ | $O(1)$         | 不稳定     | **Top-K 问题**；内存极其受限时   |